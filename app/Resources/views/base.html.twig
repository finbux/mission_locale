<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>{% block title %}Mission Locale Insertion Formation Emploi{% endblock %}</title>
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('bundles/main/css/style.css') }}"
              type="text/css" media="all" />
        <link rel="stylesheet" href="{{ asset('bundles/main/css/outdatedbrowser.css') }}"/>
    {% endblock %}
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
</head>
<body>
<!-- LE HEADER -->
{% block header %}
    <div id="header">

        <!-- LOGO MLIFE -->
        <div id="container_logo">
            <a href="{{ path('main_homepage') }}" title="Mission locale Amiens accueil">
                <span class="helper"></span>
                <img src="{{ asset('bundles/main/img/logo.png') }}" alt="">
            </a>
        </div>
        <!------------------------------->

        <!-- LOGO FACEBOOK -->
        <a href="https://www.facebook.com/MLIFE.GA" title="MLIFE facebook">
            <div id="facebook">

                <p> <img src="{{ asset('bundles/main/img/logo_facebook.png') }}" alt=""/>
                    <span class="txt_facebook">Retrouvez nous sur Facebook</span></p>
            </div>
        </a>
        <!------------------------------->

        <!-- LES TROIS BULLES -->
        <div id="bulles">
            <a href="{{ path('moins26_page') }}" title="Mission locale Amiens moins de 26 ans"><img class="img_moins26" src="{{ asset('bundles/main/img/moins26.png') }}" alt=""/></a>
            <a href="{{ path('employeur_page') }}" title="Mission locale Amiens employeur"><img class="img_employeur" src="{{ asset('bundles/main/img/employeur.png') }}" alt=""></a>
            <a href="{{ path('plus26_page') }}" title="Mission locale Amiens plus de 26 ans"><img class="img_plus26" src="{{ asset('bundles/main/img/plus26.png') }}" alt=""></a>
            <div class="cb"></div>
        </div>
        <!-------------------------------->

        <!-- BANDEAU PERSONNAGES -->
        <img id="silhouette" src="{{asset('bundles/main/img/silhouette.png') }}" />
        <!-------------------------------->

    </div>
{% endblock %}

{% block body %}
    {% block arianne %}
        <a href="{{ path('main_homepage') }}" title="Mission locale Amiens Accueil">Accueil</a>
    {% endblock %}
{% endblock %}
    <script src="{{ asset('bundles/main/js/jquery.js') }}"></script>
    <script type="text/javascript" src="http://maps.google.com/maps/api/js?sensor=false&language=fr"></script>
    <script type="text/javascript" src="{{ asset('bundles/main/js/outdatedbrowser.js') }}"></script>
    <script type="text/javascript" src="{{ asset('bundles/main/js/jquery.slides.js') }}"></script>
    <script>

        $(function(){
            $("#slides").slidesjs({
                width: 600,
                height: 300
            });
            function addLoadEvent(func) {
                var oldonload = window.onload;
                if (typeof window.onload != 'function') {
                    window.onload = func;
                } else {
                    window.onload = function() {
                        if (oldonload) {
                            oldonload();
                        }
                        func();
                    }
                }

           /* var box_arrow = $(".arrow_box");
                $("#qsn").click(function(){
                   $("#container_menu").css("margin-top","30px");
                   $(box_arrow).fadeIn();
                });
                $("#close").click(function(){
                    box_arrow.hide();
                    $("#container_menu").css("margin-top","6px");
                });*/
            }

//call plugin function after DOM ready
            addLoadEvent(function(){
                outdatedBrowser({
                    bgColor: '#f25648',
                    color: '#ffffff',
                    lowerThan: 'transform'
                })
            });
        });
    </script>
    {% block files_javascript %}

    {% endblock %}
</body>
</html>